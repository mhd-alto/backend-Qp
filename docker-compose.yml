version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: copoun-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345
      POSTGRES_DB: copoun
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d copoun"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
